apiVersion: v1
kind: Service
metadata:
  name: opstrack-backend
  namespace: opstrack
spec:
  selector:
    app: opstrack-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
